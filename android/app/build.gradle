plugins {
    id "com.android.application"
}

android {
    namespace "com.swingdesk.app"
    compileSdk 35

    defaultConfig {
        applicationId "com.swingdesk.app"
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName "1.1.0"
        buildConfigField "String", "DEFAULT_SERVER_URL", '"http://127.0.0.1:8787/"'
    }

    buildFeatures {
        buildConfig true
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.debug
        }
    }
}